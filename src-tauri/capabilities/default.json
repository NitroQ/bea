{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal Bea desktop permissions.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:webview:default",
    "updater:default",
    "process:allow-restart"
  ]
}
